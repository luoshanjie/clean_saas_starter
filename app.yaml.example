# Example runtime config.
# Copy to app.yaml in deployment and fill real values.

server:
  addr: ":9527"
  jwt_secret: "dev_secret"

database:
  driver: "postgres"
  dsn: "postgres://user:password@127.0.0.1:5432/service?sslmode=disable"
  # Reserved for upcoming SQLite bootstrap support.
  # sqlite_path: "./var/service.db"

log:
  dir: "/app/logs"
  level: "info"
  format: "json"
  console_format: "json"
  console: false

oss:
  endpoint: "127.0.0.1:9000"
  access_key: "your_minio_access_key"
  secret_key: "your_oss_secret_key"
  bucket: "service-dev"
  public_base_url: "http://127.0.0.1:9000/service-dev"
  use_ssl: false

upload_cleanup:
  enabled: true
  interval_sec: 600
  batch_size: 200
